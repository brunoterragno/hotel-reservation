import styled from 'styled-components';

export const HomeContainer = styled.main`
  display: block;
  position: relative;

  & > h3 {
    text-align: center;
    margin: 7rem 0;
    color: #434343;
    font-size: 2.3rem;
    font-weight: 400;
    letter-spacing: 0.2rem;
  }

  & > div {
    margin: 0 auto;
  }
`;
