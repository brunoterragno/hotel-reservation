import styled from 'styled-components';

export const HomeContainer = styled.main`
  position: relative;

  & > h3 {
    text-align: center;
    margin: 7rem 0;
    color: #434343;
    font-size: 2.3rem;
    font-weight: 500;
    letter-spacing: 0.2rem;
  }

  & > div {
    margin: 0 auto;
    padding: 0 1rem;
  }
`;
